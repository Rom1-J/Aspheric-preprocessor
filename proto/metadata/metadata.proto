syntax = "proto3";

package metadata;

message Metadata {
  string id = 1;
  repeated bytes emails = 2;
  repeated bytes ips = 3;
  repeated bytes domains = 4;
}

message MetadataList {
  repeated Metadata items = 1;
}